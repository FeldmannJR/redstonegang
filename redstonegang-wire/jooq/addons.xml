<?xml version="1.0" encoding="UTF-8" standalone="yes"?>
<configuration xmlns="http://www.jooq.org/xsd/jooq-codegen-3.11.0.xsd">
    <!-- Configure the database connection here -->
    <jdbc>
        <driver>com.mysql.jdbc.Driver</driver>
        <url>jdbc:mysql://localhost:3306/</url>
        <user>root</user>
        <password>senha123</password>
    </jdbc>

    <generator>
        <!-- The default code generator. You can override this one, to generate your own code style.
             Supported generators:
             - org.jooq.codegen.JavaGenerator
             - org.jooq.codegen.ScalaGenerator
             Defaults to org.jooq.codegen.JavaGenerator -->
        <name>org.jooq.codegen.JavaGenerator</name>

        <database>
            <!-- The database type. The format here is:
                 org.util.[database].[database]Database -->
            <name>org.jooq.meta.mysql.MySQLDatabase</name>

            <!-- The database schema (or in the absence of schema support, in your RDBMS this
                 can be the owner, user, database name) to be generated -->
            <inputSchema>redstonegang_survival</inputSchema>
            <outputSchemaToDefault>true</outputSchemaToDefault>
            <!-- All elements that are generated from your schema
                 (A Java regular expression. Use the pipe to separate several expressions)
                 Watch out for case-sensitivity. Depending on your database, this might be important! -->
            <includes>
                floatshop_shops
                | floatshop_transactions
                | floatshop_items
                | terrenos
                | terrenos_players
                | simplecmds_warps
                | simplecmds_locations
                | safetime
                | multiserver_tp_requests
                | multiserver_tp_teleports
                | player_inventories
                | whitelistcmd
            </includes>
            <!-- Associate data type rewrites with database columns -->
            <forcedTypes>
                <forcedType>
                    <name>BOOLEAN</name>
                    <types>tinyint</types>
                </forcedType>
            </forcedTypes>

            <!-- All elements that are excluded from your schema
                 (A Java regular expression. Use the pipe to separate several expressions).
                 Excludes match before includes, i.e. excludes have a higher priority -->
        </database>

        <target>
            <!-- The destination package of your generated classes (within the destination directory) -->
            <packageName>dev.feldmann.redstonegang.wire.game.base.database.addons</packageName>

            <!-- The destination directory of your generated classes. Using Maven directory layout here -->
            <directory>../src/main/java/</directory>
        </target>
    </generator>
</configuration>
